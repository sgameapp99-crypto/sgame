# 及時比分與比賽資訊 API 端點整理

本文檔整理前端在實作「查詢歷史比賽」、「查詢未來比賽」與「即時比分」三種功能時，所需使用的 API 端點。

---

## 📋 功能與 API 端點對應表

| 功能 | 使用的 API 端點 | 說明 |
|------|----------------|------|
| **即時比分** | `GET /api/livescore` | 查詢進行中、已結束或未開始的賽事比分與統計 |
| **未來比賽** | `GET /api/games` | 查詢尚未開始的賽事（scheduled） |
| **歷史比賽** | `GET /api/games` | 查詢已結束的賽事（finished） |

---

## 1️⃣ 即時比分（Live Score）

### API 端點
```
GET /api/livescore
```

### 功能說明
查詢即時比分資料，包含進行中的賽事分數、比賽階段、計分板詳情、即時統計與即時賠率。指定 `status` 與 `date` 時，也可取得過去與未來的賽事資訊。

### 前端頁面行為

- `LivescorePage.vue` 僅以日曆日期決定查詢模式，不再提供手動狀態切換。
- 選擇日曆日期時會自動推斷查詢條件：
  - 過去日期 → `status=finished`
  - 今日 → 同時查詢所有狀態（不帶 `status`，僅帶 `date`）
  - 未來日期 → `status=scheduled`
- 今日查詢會帶上 `date=YYYY-MM-DD` 並省略 `status`，讓後端一次回傳已開打、進行中與尚未開打的賽事；其他日期則固定帶上對應狀態。

### 查詢參數

| 參數名稱 | 型別 | 必填 | 說明 | 範例 |
|---------|------|------|------|------|
| `status` | string | 否 | 賽事狀態：`scheduled`/`live`/`finished` | `live` |
| `allianceId` | integer | 否 | 聯盟識別碼 | `1` (MLB) |
| `leagueCode` | string | 否 | 聯盟代碼 | `NBA`, `MLB`, `NPB` |
| `soccerLeagueId` | integer | 否 | 足球子聯賽識別碼 | `101` |
| `date` | string | 否 | 賽事日期（YYYY-MM-DD） | `2025-11-05` |
| `page` | integer | 否 | 分頁頁碼（預設 1） | `1` |
| `size` | integer | 否 | 每頁筆數（預設 20，最大 100） | `20` |

### 回應結構

```json
{
  "success": true,
  "data": [
    {
      "game_id": "MLB20251105001",
      "alliance_id": 1,
      "league_code": "MLB",
      "alliance_name": "美國職棒大聯盟",
      "sport_type": "BASEBALL",
      "status": "live",
      "start_time": "2025-11-05T19:05:00",
      "home_team": {
        "id": 101,
        "name": "紐約洋基",
        "pitcher": "Gerrit Cole"
      },
      "away_team": {
        "id": 202,
        "name": "西雅圖水手",
        "pitcher": "Luis Castillo"
      },
      "home_score": 4,
      "away_score": 3,
      "live_phase": "7局下",
      "scoreboard_detail": {
        "innings": [
          {"inning": 1, "home": 0, "away": 1},
          {"inning": 2, "home": 2, "away": 0}
        ]
      },
      "live_stats": {
        "pitcher": {
          "name": "Gerrit Cole",
          "pitches": 95,
          "strikeouts": 8
        },
        "outs": 1,
        "runners": ["1B", "3B"]
      },
      "odds_info": {
        "international": { /* 國際盤賠率 */ },
        "taiwan": { /* 台灣盤賠率 */ },
        "live": {
          "moneyline": {
            "home": {"odds": "1.55"},
            "away": {"odds": "2.50"}
          }
        }
      },
      "soccer_league": null,
      "last_updated": "2025-11-05T20:15:30.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "size": 20,
    "total": 6,
    "totalPages": 1,
    "hasNext": false
  }
}
```

### 使用範例

```bash
# 查詢所有進行中的賽事
curl "https://app.sportspro.tw/api/livescore?status=live"

# 查詢今日 MLB 比賽（進行中）
curl "https://app.sportspro.tw/api/livescore?status=live&allianceId=1"

# 查詢昨日 MLB 已結束賽事
curl "https://app.sportspro.tw/api/livescore?status=finished&allianceId=1&date=2025-11-04"

# 查詢明日 NBA 尚未開打賽事
curl "https://app.sportspro.tw/api/livescore?status=scheduled&leagueCode=NBA&date=2025-11-06"
```

### 重要欄位說明

| 欄位 | 型別 | 說明 |
|------|------|------|
| `live_phase` | string\|null | 比賽階段（如「7局下」、「Q3 05:21」、「67'」） |
| `scoreboard_detail` | object\|null | 計分板詳情（每局/每節得分、進球記錄等） |
| `live_stats` | object\|null | 即時統計（投手資訊、控球率、射門等） |
| `odds_info.live` | object\|null | 即時賠率（獨贏、讓分、大小分） |
| `last_updated` | string | 最後更新時間（ISO 8601 格式） |

---

## 2️⃣ 未來比賽（Scheduled Games）

### API 端點
```
GET /api/games
```

### 功能說明
查詢尚未開始的賽事，用於顯示賽程表、建立預測等。

### 查詢參數

| 參數名稱 | 型別 | 必填 | 說明 | 範例 |
|---------|------|------|------|------|
| `status` | string | 否 | 固定使用 `scheduled` 查詢未來賽事 | `scheduled` |
| `allianceId` | integer | 否 | 聯盟識別碼 | `1` (MLB) |
| `startDate` | string | 否 | 開始日期（YYYY-MM-DD） | `2025-11-05` |
| `endDate` | string | 否 | 結束日期（YYYY-MM-DD） | `2025-11-30` |
| `page` | integer | 否 | 分頁頁碼（預設 1） | `1` |
| `limit` | integer | 否 | 每頁筆數（預設 20） | `20` |

### 回應結構

```json
{
  "success": true,
  "data": [
    {
      "id": "MLB20251106001",
      "allianceId": 1,
      "alliance": {
        "id": 1,
        "code": "MLB",
        "nameZh": "美國職棒大聯盟",
        "sportType": "BASEBALL"
      },
      "gameDate": "2025-11-06",
      "gameTime": "19:05",
      "status": "scheduled",
      "homeTeamId": 101,
      "homeTeamName": "紐約洋基",
      "awayTeamId": 202,
      "awayTeamName": "波士頓紅襪",
      "homePitcher": "Gerrit Cole",
      "awayPitcher": "Chris Sale",
      "internationalOdds": {
        "spread": {
          "home": {"line": "-1.5", "odds": "1.90"},
          "away": {"line": "+1.5", "odds": "1.90"}
        },
        "total": {
          "over": {"line": "8.5", "odds": "1.88"},
          "under": {"line": "8.5", "odds": "1.92"}
        }
      },
      "taiwanOdds": {
        "moneyline": {
          "home": {"odds": "1.45"},
          "away": {"odds": "2.75"}
        }
      },
      "soccerLeagueId": null,
      "soccerLeagueName": null,
      "finalScoreHome": null,
      "finalScoreAway": null,
      "createdAt": "2025-11-05T10:00:00.000Z",
      "updatedAt": "2025-11-05T10:00:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 57,
    "totalPages": 3
  }
}
```

### 使用範例

```bash
# 查詢所有未來賽事
curl "https://app.sportspro.tw/api/games?status=scheduled"

# 查詢 MLB 本週未來賽事
curl "https://app.sportspro.tw/api/games?status=scheduled&allianceId=1&startDate=2025-11-05&endDate=2025-11-11"

# 查詢今日之後的所有賽事（分頁）
curl "https://app.sportspro.tw/api/games?status=scheduled&startDate=2025-11-05&page=1&limit=50"
```

### 重要欄位說明

| 欄位 | 型別 | 說明 |
|------|------|------|
| `gameDate` | string | 賽事日期（YYYY-MM-DD） |
| `gameTime` | string | 賽事時間（HH:mm） |
| `status` | string | 賽事狀態（`scheduled` = 未開始） |
| `homePitcher` / `awayPitcher` | string\|null | 先發投手（棒球專用） |
| `internationalOdds` | object | 國際盤賠率（讓分、大小分） |
| `taiwanOdds` | object | 台灣盤賠率（獨贏、讓分、大小分） |

---

## 3️⃣ 歷史比賽（Finished Games）

### API 端點
```
GET /api/games
```

### 功能說明
查詢已結束的賽事，包含最終比分，用於顯示戰績、統計分析、預測結果判定等。

### 查詢參數

| 參數名稱 | 型別 | 必填 | 說明 | 範例 |
|---------|------|------|------|------|
| `status` | string | 否 | 固定使用 `finished` 查詢已結束賽事 | `finished` |
| `allianceId` | integer | 否 | 聯盟識別碼 | `1` (MLB) |
| `startDate` | string | 否 | 開始日期（YYYY-MM-DD） | `2025-10-01` |
| `endDate` | string | 否 | 結束日期（YYYY-MM-DD） | `2025-10-31` |
| `page` | integer | 否 | 分頁頁碼（預設 1） | `1` |
| `limit` | integer | 否 | 每頁筆數（預設 20） | `20` |

### 回應結構

```json
{
  "success": true,
  "data": [
    {
      "id": "MLB20251104001",
      "allianceId": 1,
      "alliance": {
        "id": 1,
        "code": "MLB",
        "nameZh": "美國職棒大聯盟",
        "sportType": "BASEBALL"
      },
      "gameDate": "2025-11-04",
      "gameTime": "19:05",
      "status": "finished",
      "homeTeamId": 101,
      "homeTeamName": "洛杉磯道奇",
      "awayTeamId": 202,
      "awayTeamName": "舊金山巨人",
      "homePitcher": "Clayton Kershaw",
      "awayPitcher": "Logan Webb",
      "internationalOdds": {
        "spread": {
          "home": {"line": "-1.5", "odds": "1.90"},
          "away": {"line": "+1.5", "odds": "1.90"}
        }
      },
      "taiwanOdds": {
        "moneyline": {
          "home": {"odds": "1.55"},
          "away": {"odds": "2.45"}
        }
      },
      "finalScoreHome": 5,
      "finalScoreAway": 3,
      "soccerLeagueId": null,
      "soccerLeagueName": null,
      "createdAt": "2025-11-04T10:00:00.000Z",
      "updatedAt": "2025-11-04T22:30:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 45,
    "totalPages": 3
  }
}
```

### 使用範例

```bash
# 查詢所有已結束賽事
curl "https://app.sportspro.tw/api/games?status=finished"

# 查詢 MLB 十月份的歷史賽事
curl "https://app.sportspro.tw/api/games?status=finished&allianceId=1&startDate=2025-10-01&endDate=2025-10-31"

# 查詢昨日所有已結束的賽事
curl "https://app.sportspro.tw/api/games?status=finished&startDate=2025-11-04&endDate=2025-11-04"
```

### 重要欄位說明

| 欄位 | 型別 | 說明 |
|------|------|------|
| `status` | string | 賽事狀態（`finished` = 已結束） |
| `finalScoreHome` | integer\|null | 主隊最終得分 |
| `finalScoreAway` | integer\|null | 客隊最終得分 |
| `updatedAt` | string | 最後更新時間（通常為賽事結束時間） |

---

## 🔄 API 端點比較與選擇建議

### `/api/livescore` vs `/api/games` 的差異

| 特性 | `/api/livescore` | `/api/games` |
|------|------------------|--------------|
| **主要用途** | 即時比分查詢 | 賽事資料查詢 |
| **即時資訊** | ✅ 包含即時分數、階段、統計 | ❌ 僅基本賽事資料 |
| **計分板詳情** | ✅ 每局/每節得分 | ❌ 無 |
| **即時統計** | ✅ 投手、打者、控球率等 | ❌ 無 |
| **即時賠率** | ✅ 包含 live 賠率 | ✅ 僅開賽前賠率 |
| **歷史資料** | ⚠️ 可查詢但無詳細統計 | ✅ 完整歷史資料 |
| **未來賽事** | ⚠️ 可查詢但資訊較少 | ✅ 完整賽程資料 |
| **資料結構** | 扁平化（前端友好） | 關聯式（包含 alliance 物件） |
| **分頁參數** | `page` + `size` | `page` + `limit` |

### 使用建議

1. **即時比分頁面**
   - 使用 `/api/livescore?status=live`
   - 可獲得即時分數、比賽階段、計分板、統計資料

2. **賽程表頁面**
   - 使用 `/api/games?status=scheduled`
   - 可獲得完整的未來賽事資訊與開賽前賠率

3. **歷史戰績頁面**
   - 使用 `/api/games?status=finished`
   - 可獲得最終比分與賽事結果

4. **混合查詢（今日所有賽事）**
   - 使用 `/api/livescore?date=2025-11-05`（不指定 status）
   - 可同時獲得 scheduled、live、finished 三種狀態的賽事

---

## 📊 資料表關聯說明

### 核心資料表

1. **`games`** - 賽事基本資料
   - 儲存賽事 ID、日期、時間、隊伍、狀態、最終比分
   - 關聯 `alliances`（聯盟資料）

2. **`game_live_details`** - 即時比分詳情
   - 一對一關聯 `games.id`
   - 儲存即時分數、比賽階段、計分板、統計、即時賠率
   - 僅在賽事進行中（`status=live`）時有資料

3. **`alliances`** - 聯盟資料
   - 儲存聯盟 ID、代碼（如 MLB、NBA）、名稱、運動類型

### 資料流程

```
scheduled (未來賽事)
    ↓ 開賽
live (進行中) → game_live_details 持續更新
    ↓ 結束
finished (已結束) → finalScoreHome/Away 寫入
```

---

## 🎯 前端整合建議

### 1. 即時比分頁面

```typescript
// 查詢今日進行中的賽事
const response = await fetch('/api/livescore?status=live&date=2025-11-05');
const { data, pagination } = await response.json();

// 顯示即時比分
data.forEach(game => {
  console.log(`${game.home_team.name} ${game.home_score} : ${game.away_score} ${game.away_team.name}`);
  console.log(`比賽階段: ${game.live_phase}`);
  console.log(`最後更新: ${game.last_updated}`);
});
```

### 2. 賽程表頁面

```typescript
// 查詢本週未來賽事
const response = await fetch('/api/games?status=scheduled&startDate=2025-11-05&endDate=2025-11-11');
const { data } = await response.json();

// 顯示賽程
data.forEach(game => {
  console.log(`${game.gameDate} ${game.gameTime} - ${game.homeTeamName} vs ${game.awayTeamName}`);
});
```

### 3. 歷史戰績頁面

```typescript
// 查詢上週已結束賽事
const response = await fetch('/api/games?status=finished&startDate=2025-10-28&endDate=2025-11-03');
const { data } = await response.json();

// 顯示戰績
data.forEach(game => {
  console.log(`${game.homeTeamName} ${game.finalScoreHome} : ${game.finalScoreAway} ${game.awayTeamName}`);
});
```

### 4. 輪詢更新即時比分

```typescript
// 每 30 秒更新一次即時比分
setInterval(async () => {
  const response = await fetch('/api/livescore?status=live');
  const { data } = await response.json();
  updateLiveScores(data);
}, 30000);
```

---

## ⚠️ 注意事項

1. **即時資料更新頻率**
   - `game_live_details` 資料由後端定期更新
   - 建議前端每 30-60 秒輪詢一次 `/api/livescore?status=live`
   - 避免過於頻繁的請求造成伺服器負擔

2. **Null 值處理**
   - `live_phase`、`scoreboard_detail`、`live_stats` 在非 live 狀態時為 `null`
   - 前端需妥善處理 `null` 值，避免顯示錯誤

3. **分頁處理**
   - `/api/livescore` 使用 `page` + `size`
   - `/api/games` 使用 `page` + `limit`
   - 注意兩者參數名稱不同

4. **日期格式**
   - 統一使用 `YYYY-MM-DD` 格式
   - 時間使用 `HH:mm` 格式（24 小時制）
   - ISO 8601 格式用於 API 回應的時間戳記

5. **狀態值**
   - `scheduled` - 尚未開始
   - `live` - 進行中
   - `finished` - 已結束
   - 狀態值區分大小寫，請使用小寫

---

## 📝 版本記錄

- **2025-11-05**: 初版建立，整理三種功能的 API 端點與使用方式

